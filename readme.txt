Tech in today's world

--React
--Firebase
--Material UI
--React Router
--React context API
--REDUX
--Google Authentication
--Deploy using Firebase!

1) npm create-react-app talksapp & npm start
2) Firebase-project creation- copy config(db connection) - paste in firebase.js 
3) clean up process delete logo, app.test.js, setup
4) app.js basic body setup with css then component sidebar.js and chat.js(starts with caps) - rfce shortcut inside component(es7 snippets)
5) Install material UI for use 
6) All about material ui 
    *Iconbutton used for making the material ui icon to click.
    *To override material icon font-size !important need to be used.
    *To click all the material ui icons in the container, it's classname MuiSvgIcon-root need to be used.
    *
